kind = "native"
module = "github.com/kelvralang/window"
import_name = "window"
namespace = "github"
name = "window"
version = "0.2.0"
license = "GPL-3.0-only"
abi_version = 3
kelvra_runtime = "^0.2.0"
author = "Kelvra contributors"
description = "Official SDL2-backed native windowing package for Kelvra."
repository = "https://github.com/kelvralang/window"
dependencies = []

[native]
build = "cmake"
targets = ["linux-x86_64-gnu", "linux-arm64-gnu", "macos-arm64"]

[system-dependencies]
sdl2 = { version = ">=2.0.0", required = true }
